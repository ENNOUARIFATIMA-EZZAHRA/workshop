#include <stdio.h>
#include <string.h>

#define MAX_USERS 10

// Define the task structure
struct taches {
    int id;
    char titre[50];
    char description[50];
    char date_echeance[50];  // Corrected variable name
    char priorite[50];       // Corrected variable name
};
int main() {
    struct taches T[MAX_USERS];  // Array to store tasks
    int i = 0, op, k, j;
//Ajouter et afficher le menu des taches



int main(){

    int choix;
    do{
        printf("********MENU DES TACHES********");
        printf("Choisir une operation:\n");
        printf("1: Ajouter les tachesdu tableau\n");
         printf("2: Afficher les tachesdu tableau\n");
          printf("3:Modifier les tachesdu tableau\n");
           printf("4: Supprimer les tachesdu tableau\n");
            printf("5: Filtrer les tachesdu tableau\n");
              printf("4: Quitter le programme\n");

             scanf("%d",&choix);
             switch(choix){
                 case 1 :
                 break;
                 case 2 :
                 break;
                 case 3 :
                 break;
                 case 4 :
                 break;
                 case 5 :
                 break;
             }
    }while(1);
    getch();
    int id;
    char titre[50];
    char description[50];
    char date_echeance[50];  // Corrected variable name
    char priorite[50];
}
    void ajouter (){
        printf("donner le Id de taches : ");
        printf("donner le titre : ");
        printf("donner le description : ");
        printf("donner la date de echeance: ");
         printf("donner la priorite: ");
         scanf("%s",c.pr[c.compte].priorite);
         c.compte++;

    }
    void_afficher_tous{
        four(i=0;i<)
    }




